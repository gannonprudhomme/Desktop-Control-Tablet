import * as React from 'react';
import * as styles from './PCControls.css';

/**
 * Various shortcuts to control the desktop server
 */
const PCControls: React.FC = () => {
  const thing = 5;

  // Make this modular somehow

  return (
    <div id={styles.pcControls}>
      power
    </div>
  );
};

export default PCControls;
