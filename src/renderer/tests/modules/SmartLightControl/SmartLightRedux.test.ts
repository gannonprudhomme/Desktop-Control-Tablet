describe('SmartLight Redux', () => {
  describe('updateLight', () => {
    describe('doesn\' re-render ', () => {
      test('When the light state doesn\'t change', () => {
        // stuff
      })
    });
  });
})