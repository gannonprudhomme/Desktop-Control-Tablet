import * as React from 'react';

describe('SmartLightSlider', () => {
  // TODO: Mock smartLightSockets
  test('doesn\'t show the RGB slider if rgbCapable is false', () => {
    // arrange
  });

  // stuff
  describe('Brightness slider', () => {
    test('sets the brightness on slide', () => {
      // arrange
    });
  });
});
