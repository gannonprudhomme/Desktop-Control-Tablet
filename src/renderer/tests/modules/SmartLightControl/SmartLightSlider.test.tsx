import * as React from 'react';

describe('SmartLightSlider', () => {
  // TODO: Mock smartLightSockets
  test('doesn\'t show the RGB slider if rgbCapable is false', () => {
    // arrange
  });

  // stuff
  describe('Brightness slider', () => {
    test('sets the brightness on slide', () => {
      // arrange
    });
  });
  
  describe('doesn\'t re-render', () => {
    test('when we drag the slider and the light info is updated within 50ms', () => {

    });
  });

  describe('shows the hue slider', () => {
    test('when the temperature slider is showing and the hue updates externally', () => {

    });
  });

  describe('shows the temperature slider', () => {
    test('when the hue slider is showing and the temperature updates externally', () => {

    });
  });
});
