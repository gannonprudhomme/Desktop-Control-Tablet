import * as React from 'react';

describe('HueSlider', () => {
  test('calls onChange prop on slide', () => {
    // arranger
  });

  test('takes in the hue prop and updates when it\'s changed', () => {
    // arrange
  });
});
